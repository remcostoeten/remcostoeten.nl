---
title: "Testing Table Parsing"
excerpt: "A test post to verify that markdown tables are properly parsed and rendered."
publishedAt: "2024-01-20"
tags: ["Test", "Markdown", "Tables"]
category: "development"
status: "published"
author: "Remco Stoeten"
seo:
  title: "Testing Table Parsing in MDX"
  description: "A test post to verify that markdown tables are properly parsed and rendered in our MDX system."
  keywords: ["test", "markdown", "tables", "MDX"]
---

# Testing Table Parsing

This is a test post to verify that markdown tables are properly parsed and rendered.

## Sample Table

Here's a sample table to test the table parsing:

| Feature | Status | Notes |
|---------|--------|-------|
| Blog Posts | ✅ Working | Filesystem-based |
| Tables | 🔄 Testing | Should render properly |
| SEO | ✅ Working | Full metadata support |
| Animations | ✅ Working | Framer Motion |

## Another Table Test

Let's try a different table format:

| Technology | Version | Purpose |
|------------|---------|---------|
| Next.js | 15 | React Framework |
| TypeScript | 5.0+ | Type Safety |
| Tailwind CSS | 3.4+ | Styling |
| MDX | Latest | Content |

## Code Block Test

Here's also a code block to test syntax highlighting:

```javascript
function testTableParsing() {
  const table = {
    headers: ['Feature', 'Status', 'Notes'],
    rows: [
      ['Blog Posts', '✅ Working', 'Filesystem-based'],
      ['Tables', '🔄 Testing', 'Should render properly']
    ]
  };
  
  return table;
}
```

## Conclusion

If you can see this table properly formatted with borders and styling, then the table parsing is working correctly!